language: node_js
sudo: false
node_js:
  - '4.0'
  - '4.1'
  - '4.2'
  - '4.3'
cache:
  directories:
    - $(npm root -g)

branches:
  only:
    - master
    - dev
    - stage
    - test
    - update_test

before_install:
  - cp bin/test/package.json .
  - npm run prepare

after_success:
  - npm run coverage
